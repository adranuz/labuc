{
  "name": "equality-conciliacion",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "api",
    "app"
  ],
  "main": "index.js",
  "scripts": {
    "build:api": "npm run build --workspace=api",
    "build:app": "npm run build --workspace=app",
    "start:api": "npm run start --workspace=api",
    "start:app": "npm run start --workspace=app",
    "generate": "cd api && npx prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
